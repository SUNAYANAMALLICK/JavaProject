package coding.exercises.infosys;

import core.inheritance.employee.Employee;

public class CodingTest {
    public static void main(String[] args) {

        // ArrayList with Employee (name, id) --> map (id, name)

      //  Map <Integer, String> map = list.stream().collect(Collectors.toMap(Employee::getId,Employee::getName));

        // Excel file -- sheet no, cell no
        // insert into a table
        // 1 mil
        // insert into the table

        // Apache Poi
        // Open CSV

        // Batch job
        // Structure of the table
        // Dependency -- JPA, MySql
        // Configuration --
        // Entity, Repository

        // Database -- Dev, Sit, Uat, Prod

        // Request -> Api Key

        // @Configuration

        // 2 db --> qualifier()

        // ssl

        // http/https

        // select * from (select * from emp order by sal desc) where rownum  = 2;

        // connect by

        // interface payment --> 3 impl

        // factory --> () --> 3 impl

        //



    }
}
